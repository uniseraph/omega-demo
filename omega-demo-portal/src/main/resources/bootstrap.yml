spring:
  application:
    name: omega-demo-portal
  cloud:
    config:
      discovery:
        enabled: false
        serviceId: omega-framework-configserver
      profile: dev
      label: master

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
